{
  "name": "warcraft-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Static systems mockups for the Warcraft-inspired incremental game.",
  "scripts": {
    "dev": "node dev-server.mjs",
    "test": "npm run integration:test && npm run combat:test",
    "integration:test": "node --test scripts/tests/*.test.mjs",
    "combat:test": "node mockup/combat/smoke-test.js",
    "format": "npx --yes prettier@3 --write mockup scripts dev-server.mjs README.md AGENTS.md IMPLEMENTATION_PLAN.md"
  },
  "engines": {
    "node": ">=20.12"
  },
  "type": "module"
}
